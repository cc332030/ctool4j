
# 规范

严格使用本规范

遵守《阿里巴巴Java开发手册》；与本项目规范冲突时，以本项目定义规范为准

## 通用规范

### 执行原则

- 指令执行：严格执行用户指令，不擅自扩大改动范围（要求 A 时不额外改 B；关联改动先说明并征得同意；回退自作主张的改动时只回退、不加新内容）；需求有歧义时先提问澄清，不自行假设后动手；每次操作后必须验证是否达到预期（如用 git diff/status 比对、运行检查等确认结果）
- 编译验证：编译/运行验证耗时较长，不滥运行——仅当确需验证（用户明确要求，或存在仅能靠运行确认的行为）时才执行；多个待验证项能合并时合并为一次执行，不逐项独立验证
- 任务编排与上下文管理：互不影响的任务拆分并发执行，互相影响或需共享状态的必须串行，多任务先易后难；大范围检查及多步骤任务（调研、设计、编码、写用例、用例验证、文档等）的子 agent 拆分协作细节见 agent 协作规范；大规模检查可能上下文不足时，进度与结果写入临时文档：不保留、只做问题记录、不写备注（备注一律写入代码注释等持久文档），问题解决后可在其中更新解决状态，持久文档不得引用临时文档及其中的内容（如问题编号、问题描述等；临时文档会删除，其内容无出处可追溯）；整个项目级别内容按子模块拆分、分步分批处理；后续任务不依赖当前上下文即可释放（清除历史上下文或开新会话）
- 分步验证：设计、开发、写用例测试等多步骤任务，每个步骤执行完须对涉及的部分做一次隔离上下文的 code review，确保每步正确——前面方向错了，后面做再多也是无用功

### 文档

- 注释与文档：不写死常量数值，用抽象描述或引用常量名；代码注释仅记录局部决策与要点（如初次设计、反复修改、有争议时的方案依据与设计思路：为什么这样做、为什么不做其他方案、接受了什么边界），不追求完整设计叙述——完整设计（功能怎么设计、用例考虑、兜底、适用范围等）统一写入设计文档（见"设计文档"条目），避免注释东一块西一块不完整；常规小改动不强制备注；总纲：一切有价值的内容都需记录——设计决策、方案取舍、约束边界、兼容性考量、测试设计、问题处理等，按内容性质写入合适的持久位置（设计文档、代码注释/javadoc、README、规范文件等），不遗漏、不依赖记忆，临时文档除外（进度记录、扫描中间结果等按"任务报告"条目规则不保留、不提交）
- 设计文档：核心功能的新增、重构（含行为变更、方案选型、有取舍的设计）必须编写或更新设计文档，记录当时考虑到的所有东西：功能设计（方案是什么、为什么这样做、备选方案及不选原因）、测试设计（测试行为本身的设计：测试架构与组织、测试手法与参考实现、覆盖哪些场景、为什么这样测、测试边界与取舍）、兜底（有哪些兜底、兜底触发条件与边界）、适用范围（适用场景、不适用场景、已知限制与取舍）；功能设计与测试设计放在同一设计文档内（测试是功能的验证规格，二者一体，避免文档分散）；推荐位置：根目录 `doc/design/`（如 `doc/design/<功能名>.adoc`，AsciiDoc 格式），跨模块设计放根目录、模块内单一功能可放模块 `doc/design/`；文档随代码演进同步更新，新增、重构时必须处理
- 文件格式与编码：除 AGENTS.md 外，其他文档优先使用 AsciiDoc（.adoc）格式；所有文件一律 UTF-8 编码，除非明确不是 UTF-8（如特定二进制格式、明确指定其他编码的文件）
- 文档代码块：md/adoc 文档中代码块的语言标识须与实际内容匹配——代码块虽不要求编译通过，但必须满足所标识语言的基本语法（避免 IDE 语法检查报红）；无法保证语法正确时标识为无格式类型（如 `text`），优先使用带格式的准确语言标识（如 `java`、`xml`、`bash`）；伪代码/结构描述等非语法内容不得标注具体语言
- 文档问题：javadoc、注释、README 等纯文档性缺陷，发现后直接修复，无需问询

### 代码质量

- 类重名：除非类全名（全限定类名）冲突不可避免，否则不使用类重名；发现类重名的需调整优化
- NPE 原则：NPE 问题原则上必须修复（存在取舍时，在代码注释中说明取舍依据与边界）
- 兼容性与平衡：Java 跨平台，不得因局部小优化牺牲兼容性（JDK 版本、操作系统、环境依赖等）；优化与健壮性、可维护性、实用性之间保持平衡，目标始终是更好维护、代码更健壮、实用性更好

### code review

- code review 问题记录：分级用 P 前缀，P0 最高、数字越小级别越高；序号以 Q 开头跨分级连续编号（如 P0 编 Q1~Q3 则 P1 从 Q4 开始），级别仅作分类不影响编号；每个问题浓缩一句话标题；问题需有明确状态（待处理/已修复/已备注/待评估等），已备注问题写明处理方案，便于人工更新（适用于所有有状态、需人工确认的问题记录）；废弃方法、字段及待完成占位代码（TODO、空实现、未引用方法等）不作问题提示；过于细节、无实际影响的问题不作提示（矫情原则）；上述不作问题提示的规则同样适用于测试类
- 修复 review 问题：只做最小目标改动，修复前后自查，不得引入新的行为变更或缺陷；修复前先声明该功能的"不可违反的约束"（不变量），候选方案必须全部满足；候选方案做正常路径与异常路径双检查（正常路径行为不得改变、异常路径资源须释放）；修复后反向自查：本次修复会让哪个原本正常的场景失效？答不上来视为未自查；修复完补充测试用例且验证通过

### 规范管理

- 规范分层：规范只收录通用规则；局部决策/已知取舍写在对应代码注释，不写入全局规范
- 规范组织：规范须归纳总结、分组分类组织（按主题分组，如执行原则/文档/代码质量/code review/规范管理/任务与 git），不得遇到情况就零散追加、平铺堆砌——新增或调整规范时先检查是否与现有条目重复、可归纳合并，冗余内容须合并去重；条目按主题归类到对应分组，不重复表述
- 规范调整：除非明确要求删除某条规范，否则调整规范时必须保证规范完整性（不因调整丢失规则）；调整完成后须验证（如核对要点是否全保留），验证需在隔离的上下文（如干净的子 agent）中进行，避免本次调整的上下文影响验证结果

### 任务与 git

- 任务报告：大范围任务（全项目 code review、多模块改造等）完成后必须生成文件形式的结果报告（如 script/check-result-*.md），内容须含检查范围、问题清单（按分级编号规则）与处理状态；结果报告不属于需持久化的文件，无需 git add、不提交；临时过程文档（进度记录、扫描中间结果等）不保留、不提交，持久文档不得引用临时文档及其中的内容（如问题编号、问题描述等；临时文档会删除，其内容无出处可追溯）
- git 操作：新生成且需保留的文件，默认执行 git add 加入暂存区；文件移动必须使用 git mv（文件编辑+移动时须保留跟踪历史，避免 git 将移动识别为删除+新增）；git add/commit 前须检查暂存内容，确认无临时文件（临时过程文档、扫描中间产物、构建产物等）被误提交；若发现临时文件已被提交，直接执行 git rm 删除，不得执行 git commit，由用户最终自行提交确认，不得因此中断任务

## 具体规范

- java 规范：[AGENTS-JAVA.MD](agent/AGENTS-JAVA.MD)
- spring 规范：[AGENTS-SPRING.MD](agent/AGENTS-SPRING.MD)
- maven 规范：[AGENTS-MAVEN.MD](agent/AGENTS-MAVEN.MD)
- 语法糖规范：[AGENTS-SYNTAX.MD](agent/AGENTS-SYNTAX.MD)
- 通用编码规范：[AGENTS-CODING.MD](agent/AGENTS-CODING.MD)
- 测试规范：[AGENTS-TEST.MD](agent/AGENTS-TEST.MD)
- agent 协作规范：[AGENTS-AGENT.MD](agent/AGENTS-AGENT.MD)
