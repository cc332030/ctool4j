
# 规范

严格使用本规范

## 通用规范

- 严格执行用户指令，不擅自扩大改动范围：要求 A 时不额外改 B；关联改动须先说明并征得同意；回退自作主张的改动时只回退，不加新内容
- 需求有歧义时先提问澄清，不自行假设后动手
- 注释/文档不写死常量的具体数值（如"5 秒"），用抽象描述或引用常量名，避免常量变更后与实际不一致
- 备注方案和设计思路：仅在初次设计、反复修改、有争议时，才在对应代码注释中记录方案依据与设计思路，说明"为什么这样做、为什么不做其他方案、接受了什么边界"，避免后来者误判为冗余而误改；常规小改动不强制备注
- 严禁反复执行 mvnw 编译验证；仅在用户明确要求时才编译验证
- 规范只收录通用规则；局部决策/已知取舍写在对应代码注释，不写入全局规范（如"某处字段覆盖行为"应写在类/方法注释，而非 AGENTS.md）
- code review 等操作进行问题分级时，使用 P 开头的级别，P0 最高（如 P0/P1/P2），数字越小级别越高
- 问题序号以 Q 开头（如 Q1/Q2/Q3），跨分级时连续编号（如 P0 编号 Q1~Q3，则 P1 从 Q4 开始），级别仅作分类，不影响编号连续性
- 除序号外，再为每个问题浓缩总结出一个简短标题（一句话概括问题核心），避免修复时搞错问题
- 修复 review 问题时只做最小目标改动，修复前后自查，不得引入新的行为变更或缺陷（新 bug）
- 修复前先声明该功能的"不可违反的约束"（不变量），候选方案必须全部满足，不得只满足被修的问题而破坏其他约束
- 候选方案须做正常路径与异常路径双检查：正常路径行为不得改变，异常路径资源须释放
- 修复后反向自查：本次修复会让哪个原本正常的场景失效？答不上来视为未自查
- 大规模 code review 等检查可能造成上下文不足时，优先将进度和结果写入文件；检查/进度文档属临时文档（最终不保留），只做问题记录，不更新修复状态、不写备注（备注一律写入代码注释或其他持久文档）；持久文档不得引用临时文档，避免残留悬空引用
- 当前项目为工具类：几乎所有类供业务系统消费，当前项目内无调用方属正常（工具库为业务侧提供服务）
- 当前项目子模块较多：处理整个项目级别的内容时，尽量按子模块拆分、分布处理；新任务如不依赖本次上下文，可直接开新会话或清除历史上下文，避免上下文长度不足

## 具体规范

- java 规范：[AGENTS-JAVA.MD](agent/AGENTS-JAVA.MD)
- spring 规范：[AGENTS-SPRING.MD](agent/AGENTS-SPRING.MD)
- maven 规范：[AGENTS-MAVEN.MD](agent/AGENTS-MAVEN.MD)
- 语法糖规范：[AGENTS-SYNTAX.MD](agent/AGENTS-SYNTAX.MD)
- 通用编码规范：[AGENTS-CODING.MD](agent/AGENTS-CODING.MD)
