
# 规范

严格使用本规范

## 通用规范

- 指令执行：严格执行用户指令，不擅自扩大改动范围（要求 A 时不额外改 B；关联改动先说明并征得同意；回退自作主张的改动时只回退、不加新内容）；需求有歧义时先提问澄清，不自行假设后动手；每次操作后必须验证是否达到要求即预期（如用 git diff/status 比对、运行检查等确认结果）
- 注释与文档：不写死常量的具体数值，用抽象描述或引用常量名，避免常量变更后与实际不一致；仅在初次设计、反复修改、有争议时，才在代码注释中记录方案依据与设计思路（为什么这样做、为什么不做其他方案、接受了什么边界），常规小改动不强制备注
- 文档格式：除 AGENTS.md 外，其他文档优先使用 AsciiDoc（.adoc）格式
- 编译验证：严禁反复执行 mvnw 编译验证，仅在用户明确要求时才编译验证
- 规范分层：规范只收录通用规则；局部决策/已知取舍写在对应代码注释，不写入全局规范
- code review 问题记录：分级用 P 前缀，P0 最高、数字越小级别越高；序号以 Q 开头跨分级连续编号（如 P0 编 Q1~Q3 则 P1 从 Q4 开始），级别仅作分类不影响编号；每个问题浓缩一句话标题，避免修复时搞错问题；问题需有明确状态（待处理/已修复/已备注/待评估等），已备注问题需写明解决方案与处理方案，便于人工更新方案与状态（此规则同样适用于其他有状态、需人工确认的问题记录，不限于 code review 文档）；废弃方法、字段不参与 code review（不提示问题），因随时会被移除；项目里待完成的占位代码（TODO、空实现、未引用方法等）不作问题提示，后续迭代自行补齐；过于细节、无实际影响的问题不作提示（矫情原则）
- 修复 review 问题：只做最小目标改动，修复前后自查，不得引入新的行为变更或缺陷；修复前先声明该功能的"不可违反的约束"（不变量），候选方案必须全部满足；候选方案做正常路径与异常路径双检查（正常路径行为不得改变、异常路径资源须释放）；修复后反向自查：本次修复会让哪个原本正常的场景失效？答不上来视为未自查；问题修复完需要补充测试用例且需要验证通过
- 上下文管理：大规模 code review 等检查可能造成上下文不足时，优先将进度和结果写入文件；检查/进度文档属临时文档（最终不保留），只做问题记录，不写备注（备注一律写入代码注释或其他持久文档）；问题解决后可在其中更新解决状态；持久文档不得引用临时文档；处理整个项目级别的内容时按子模块拆分、分步分批处理，新任务如不依赖本次上下文可直接开新会话或清除历史上下文
- 并行处理：一次给多个任务且互相不影响时，拆分多个 agent 并发处理提高效率；任务改动互相影响或需共享状态的必须串行，避免冲突；多个任务时先评估各自难度，简单的先做（易先难后，先建立完成度与信心，也便于复杂任务复用已完成的简单成果）；简单任务完成后及时释放上下文（清除历史上下文或开新会话），为后续复杂任务节省上下文

## 具体规范

- java 规范：[AGENTS-JAVA.MD](agent/AGENTS-JAVA.MD)
- spring 规范：[AGENTS-SPRING.MD](agent/AGENTS-SPRING.MD)
- maven 规范：[AGENTS-MAVEN.MD](agent/AGENTS-MAVEN.MD)
- 语法糖规范：[AGENTS-SYNTAX.MD](agent/AGENTS-SYNTAX.MD)
- 通用编码规范：[AGENTS-CODING.MD](agent/AGENTS-CODING.MD)
- 测试规范：[AGENTS-TEST.MD](agent/AGENTS-TEST.MD)
- agent 协作规范：[AGENTS-AGENT.MD](agent/AGENTS-AGENT.MD)
