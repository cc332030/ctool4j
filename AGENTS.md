
# 规范

严格使用本规范

遵守《阿里巴巴Java开发手册》；与本项目规范冲突时，以本项目定义规范为准

## 通用规范

### 执行原则

- 指令执行：严格执行用户指令；需求有歧义时先提问澄清，不自行假设后动手；每次操作后必须验证是否达到预期（如用 git diff/status 比对、运行检查等确认结果）
- 命令执行：能用 cmd（批处理/命令提示符）直接运行的命令（如 `mvnw.cmd`、`mvnw`、`git`、`javac` 等）优先用 cmd 直接执行，不套用 powershell 等更重的 shell——避免不必要的环境差异与转义问题（powershell 会对参数做额外解析，如 `-Dxxx.y=z` 的 `.` 可能被当成管道/阶段分隔）；确需 powershell 特性（如复杂对象处理、`Select-String` 过滤等）时才用 powershell
- 指令完整性：用户说的每一句话、每一个字都是有效指令，不允许忽略、跳过或选择性执行任何一个字（仅明显打错字除外）；执行时逐字对照用户输入，确认全部落实后方可结束
- 先规划后执行：任何任务动手前必须先规划——明确目标与范围、拆解执行步骤、设计实现方案（复杂/有取舍的任务含备选方案与取舍评估），规划完成后再执行；规划文档须落到磁盘（如设计文档或临时规划文件），不得仅存于上下文（避免超出上下文限制）；规划文档是否需持久化按任务类型判断——项目代码开发等需提交的任务，规划按设计文档持久化（见"设计文档"条目），后续随代码一并提交；一般任务规划写入 `tmp/` 文件夹临时文档（不保留、不提交），方案在代码注释或执行说明中体现；执行中偏离规划时先调整规划再继续，不得边做边定方案
- 范围控制：原则上不得范围蔓延，执行范围以规划的目标与范围为界——不擅自扩大改动范围（要求 A 时不额外改 B；关联改动先说明并征得同意；回退自作主张的改动时只回退、不加新内容）；执行过程中发现前期规划有问题或需补充的新内容：先记录下来（如临时文档、问题清单），不中断当前任务，待当前任务完成后再考虑修复或补充，完成后须提醒用户；默认不中断任务，除非过程中发现严重偏移目标（需大幅变更方案、明显超出原任务边界）——严重偏移目标时：当前任务完成后直接停止，无需先向用户确认，由用户人工确认后再决定是否继续或调整
- 任务编排与上下文管理：互不影响的任务拆分并发执行，互相影响或需共享状态的必须串行，多任务先易后难；大范围检查及多步骤任务（调研、设计、编码、写用例、用例验证、文档等）的子 agent 拆分协作细节见 agent 协作规范；大规模检查可能上下文不足时，进度与结果写入 `tmp/` 文件夹临时文档：不保留、只做问题记录、不写备注（备注一律写入代码注释等持久文档），问题解决后可在其中更新解决状态，持久文档不得引用临时产物及其中的内容（如问题编号、问题描述等；临时产物会删除，其内容无出处可追溯）；整个项目级别内容按子模块拆分、分步分批处理；后续任务不依赖当前上下文即可释放（清除历史上下文或开新会话）
- 任务放弃与中断：任务在执行中被中断、且用户后续未明确提及继续该任务时，该任务视为放弃——用户改变主意或另起新任务时同理；放弃的判断以用户最新指令为准，不自行推断延续；放弃后不再做任何事（含还原/撤销已实施改动），由用户人工处理；已放弃/中断的任务不再考虑，不得再次追问确认是否继续、不得重新提起或自行延续（用户主动再次发起该任务时除外）

### 规范管理

- 规范分层：规范只收录通用规则；局部决策/已知取舍写在对应代码注释，不写入全局规范
- 规范组织：规范须归纳总结、分组分类组织（按主题分组，如执行原则/文档/代码质量/code review/规范管理/任务与 git），不得遇到情况就零散追加、平铺堆砌——新增或调整规范时先检查是否与现有条目重复、可归纳合并，冗余内容须合并去重；条目按主题归类到对应分组，不重复表述
- 规范调整：除非明确要求删除某条规范，否则调整规范时必须保证规范完整性（不因调整丢失规则）；调整完成后须验证（如核对要点是否全保留），验证需在隔离的上下文（如干净的子 agent）中进行，避免本次调整的上下文影响验证结果

## 具体规范

- java 规范：[AGENTS-JAVA.MD](agent/AGENTS-JAVA.MD)
- spring 规范：[AGENTS-SPRING.MD](agent/AGENTS-SPRING.MD)
- maven 规范：[AGENTS-MAVEN.MD](agent/AGENTS-MAVEN.MD)
- 语法糖规范：[AGENTS-SYNTAX.MD](agent/AGENTS-SYNTAX.MD)
- 通用编码规范：[AGENTS-CODING.MD](agent/AGENTS-CODING.MD)
- 测试规范：[AGENTS-TEST.MD](agent/AGENTS-TEST.MD)
- 验证规范：[AGENTS-VERIFY.MD](agent/AGENTS-VERIFY.MD)
- 文档规范：[AGENTS-DOC.MD](agent/AGENTS-DOC.MD)
- code review 规范：[AGENTS-REVIEW.MD](agent/AGENTS-REVIEW.MD)
- 任务与 git 规范：[AGENTS-GIT.MD](agent/AGENTS-GIT.MD)
- agent 协作规范：[AGENTS-AGENT.MD](agent/AGENTS-AGENT.MD)
