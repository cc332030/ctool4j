= CNotEmptyAnnotationPlugin 测试用例文档

Version: 1.0
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.doc.openapi2.plugins.parameter.impl.CNotEmptyAnnotationPlugin`（openapi2 模块）——@NotEmpty 校验注解 → 参数必填。

对应测试类：`com.c332030.ctool4j.doc.openapi2.plugins.parameter.impl.CNotEmptyAnnotationPluginTests`

对应设计文档：`doc/design/openapi2/CNotEmptyAnnotationPlugin.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CNotEmptyAnnotationPlugin 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号，文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 覆盖注解类型识别、supports 支持性。
- 覆盖 apply 分支输出：命中 @NotEmpty 标记必填（正例）、未命中不标记（反例）。

=== 设计依据

- 依据功能设计对"命中 @NotEmpty 标记必填、isRequired 默认 true"的约定。
- 依据白盒/黑盒原则与分支覆盖：命中/未命中两种分支输出均验证。

=== 覆盖场景与未覆盖

- 覆盖：getAnnotationClass、supports（含 null）、apply 命中/未命中。
- 未覆盖：在真实 springfox 文档生成链路上的端到端行为。

== 1. 注解识别与支持性

- 1.1 返回 NotEmpty.class（getAnnotationClass）
- 1.2 支持 SWAGGER_2/12（supports）
- 1.3 null 文档类型也支持（supports_null）

== 2. apply 分支输出

- 2.1 命中 @NotEmpty 标记参数必填（apply_hitAnnotation）
- 2.2 未命中 @NotEmpty 不标记必填（apply_missAnnotation）

== 变更日志

- 1.0 | 2026-08-21 | 初始落地
