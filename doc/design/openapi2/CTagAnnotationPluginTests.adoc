= CTagAnnotationPlugin 测试用例文档

Version: 1.0
LastModified: 2026-09-04
所属功能：`com.c332030.ctool4j.doc.openapi2.plugins.operation.impl.CTagAnnotationPlugin`（openapi2 模块）——@CTag → 接口分组 tag。

对应测试类：`com.c332030.ctool4j.doc.openapi2.plugins.operation.impl.CTagAnnotationPluginTests`

对应设计文档：`doc/design/openapi2/CTagAnnotationPlugin.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CTagAnnotationPlugin 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号，文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 覆盖 supports 支持性。
- 覆盖 apply 分支输出：类级 @CTag 作为分组（正例）、合并方法级 @COperation.tags、无 @CTag 不处理（反例）。

=== 设计依据

- 依据功能设计对"分组 = 类级 name 并集方法级 tags"的约定。
- 依据分支覆盖：有/无 @CTag、是否合并方法级分组均验证。

=== 覆盖场景与未覆盖

- 覆盖：supports（含 null）、apply 类级分组/合并/无注解。
- 未覆盖：在真实 springfox 文档生成链路上的端到端分组行为。

== 1. 支持性

- 1.1 支持 SWAGGER_2/12 与 null（supports）

== 2. apply 分支输出

- 2.1 类级 @CTag.name 作为分组 tag（apply_classTag）
- 2.2 合并方法 @COperation.tags 到分组（apply_mergeOperationTags）
- 2.3 无 @CTag 不设置分组（apply_noAnnotation）

== 变更日志

- 1.0 | 2026-09-04 | 初始落地
