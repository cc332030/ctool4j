= CTextEnumTestController 设计文档

Version: 1.0
LastModified: 2026-09-06
模块：ctool4j-doc-openapi2
类：com.c332030.ctool4j.doc.openapi2.test.controller.CTextEnumTestController
状态：已落地
适用范围：枚举 text 展示集成测试辅助 Controller

== 1. 功能说明

`CTextEnumTestController` 为枚举 text 展示集成测试辅助 Controller（@RestController + @CTag 供 Docket 收集）：
枚举字段/参数均未标注 @CSchema/@CParameter，故 text 说明会写入 description（验证「允许值=枚举名、text 进 description」）：

- `body(@RequestBody CTextEnumTestDTO)`：POST `/c-text-enum/body`，DTO 含实现 `ICText` 的枚举字段
  （验证 model 字段枚举允许值可提交、description 展示 text）。
- `query(@RequestParam CRequestHeaderEnum header)`：POST `/c-text-enum/query`，枚举请求参数
  （验证 query 参数枚举允许值可提交、description 展示 text）。

== 4. 适用范围

=== 4.1 适用场景

- `CTextEnumIntegrationTests` 集成测试，作为真实接口入口验证 model 字段与 query 参数枚举展示。

== 5. 已知限制与取舍

- 测试辅助类；仅提供两个测试接口。

== 变更日志

- 1.0 | 2026-09-06 | 初始落地
