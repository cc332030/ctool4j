= COperationAnnotationPlugin 测试用例文档

Version: 1.0
LastModified: 2026-09-04
所属功能：`com.c332030.ctool4j.doc.openapi2.plugins.operation.impl.COperationAnnotationPlugin`（openapi2 模块）——@COperation → operation 摘要/说明/废弃标记。

对应测试类：`com.c332030.ctool4j.doc.openapi2.plugins.operation.impl.COperationAnnotationPluginTests`

对应设计文档：`doc/design/openapi2/COperationAnnotationPlugin.adoc`（功能设计，测试内容以本文档为准）。

本文档为 COperationAnnotationPlugin 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号，文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 覆盖 supports 支持性。
- 覆盖 apply 分支输出：命中 @COperation 写入 summary/description/deprecated（正例）、未命中不处理（反例）。

=== 设计依据

- 依据功能设计对"summary/description 非空覆盖、deprecated=true 标记废弃"的约定。
- 依据白盒/黑盒原则与分支覆盖：命中/未命中两种分支输出均验证。

=== 覆盖场景与未覆盖

- 覆盖：supports（含 null）、apply 命中/未命中。
- 未覆盖：在真实 springfox 文档生成链路上的端到端行为。

== 1. 支持性

- 1.1 支持 SWAGGER_2/12 与 null（supports）

== 2. apply 分支输出

- 2.1 命中 @COperation 写入 summary/description/deprecated（apply_hitAnnotation）
- 2.2 未命中 @COperation 不处理（apply_missAnnotation）

== 变更日志

- 1.0 | 2026-09-04 | 初始落地
