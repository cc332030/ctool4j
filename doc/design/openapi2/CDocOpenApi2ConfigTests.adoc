= CDocOpenApi2Config 测试用例文档

Version: 1.0
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.doc.openapi2.config.CDocOpenApi2Config`（openapi2 模块）——OpenAPI2 文档配置属性。

对应测试类：`com.c332030.ctool4j.doc.openapi2.config.CDocOpenApi2ConfigTests`

对应设计文档：`doc/design/openapi2/CDocOpenApi2Config.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CDocOpenApi2Config 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号，文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 覆盖 pathMapping 的默认值、setter 正常赋值、setter 置 null。

=== 设计依据

- 依据功能设计对"pathMapping 默认 /"的约定。
- 依据黑盒原则：默认值、正常赋值、null 赋值均覆盖。

=== 覆盖场景与未覆盖

- 覆盖：默认值、设置值、置 null。
- 未覆盖：`@ConfigurationProperties` 从配置文件绑定的端到端行为（需 Spring 上下文）。

== 1. pathMapping

- 1.1 默认值为 "/"（defaultPathMapping）
- 1.2 set 后生效（setPathMapping）
- 1.3 set null 后为 null（setPathMapping_null）

== 变更日志

- 1.0 | 2026-08-21 | 初始落地
