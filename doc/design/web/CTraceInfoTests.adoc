= CTraceInfo 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.web.model.model.CTraceInfo`（web 模型类）——跟踪信息实体。

对应测试类：`com.c332030.ctool4j.web.test.model.model.CTraceInfoTests`

对应设计文档：`doc/design/web/CTraceInfo.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CTraceInfo 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「构造 / builder / setter/getter」多个维度组织，验证 Lombok @Data/@SuperBuilder 行为。

=== 设计依据

- 依据功能设计对 @Data/@SuperBuilder 生成方法的约定。

=== 覆盖场景与未覆盖

- 覆盖：无参/全参构造；builder；setter/getter。
- 未覆盖：无。

== 1. 构造与取值

- 1.1 无参构造：traceId 为 null（noArgsConstructor）
- 1.2 全参构造：traceId 正确（allArgsConstructor）
- 1.3 builder：traceId 正确（builder）
- 1.4 setter/getter：设置与读取（setterAndGetter）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
