= CWebInit 设计文档

Version: 1.0
LastModified: 2026-08-21
模块：ctool4j-web
类：com.c332030.ctool4j.web.configuration.CWebInit
状态：已落地

== 1. 功能说明

`CWebInit` 为 web 模块 Spring 启动初始化回调，`@Component` + 实现 `ICSpringInit`，
`onInit()` 在 Spring 启动时被调用。

核心方法 `onInit()`：

- 当前无处理逻辑（空实现）。

== 2. 设计要点

=== 2.1 生命周期回调

- 通过 `ICSpringInit` 接入 Spring 生命周期，`onInit` 在容器初始化完成时回调。

== 3. 兜底设计

| 场景 | 兜底行为 |
|------|---------|
| onInit 当前为空 | 无实际行为，仅作为扩展点 |

== 4. 适用范围

=== 4.1 适用场景

- 预留 web 模块 Spring 启动初始化扩展点。

=== 4.2 不适用/边界场景

- 当前无实际初始化逻辑。

== 5. 已知限制与取舍

- 空实现仅作扩展点占位，未来 web 模块需要启动初始化时可在此补充。

测试内容：无测试类（空实现，无行为可测）。

== 变更日志

- 1.0 | 2026-08-21 | 初始落地
