= ICTraceInfo 设计文档

Version: 1.0
LastModified: 2026-08-21
模块：ctool4j-web
类：com.c332030.ctool4j.web.model.model.ICTraceInfo
状态：已落地

== 1. 功能说明

`ICTraceInfo` 为链路追踪信息接口，声明 `traceId` 的读写方法：

- `getTraceId()`：获取链路追踪 ID
- `setTraceId(String)`：设置链路追踪 ID

== 2. 设计要点

=== 2.1 统一追踪模型

- 定义携带链路追踪 ID 的模型契约，供 `CTraceInfo` 等实现。

== 3. 兜底设计

| 场景 | 兜底行为 |
|------|---------|
| 无 | 纯接口契约 |

== 4. 适用范围

=== 4.1 适用场景

- 需要携带链路追踪 ID 的模型类实现本接口。

== 5. 已知限制与取舍

- 仅声明读写方法，无默认实现。

测试内容：无独立测试类（接口，由实现类测试间接覆盖）。

== 变更日志

- 1.0 | 2026-08-21 | 初始落地
