= CSchema 设计文档

Version: 1.4
LastModified: 2026-09-08
模块：ctool4j-definition
类：com.c332030.ctool4j.doc.annotation.CSchema
状态：已落地
总体文档：`doc/design/definition/CDocAnnotation.adoc`（接口文档注解族）

== 1. 功能说明

`CSchema` 为接口属性文档描述注解，语义与原生 `@ApiModelProperty` / OpenAPI3 `@Schema` 一致
（仅文档，不参与运行时校验），可标注在字段、getter 方法上（`@Target`：FIELD / METHOD，
参数级描述用 `@CParameter`）。

- `value()`：属性描述（对应 `ApiModelProperty.value` / `Schema.description`），默认 ""

需要必填校验时配合 `CRequired` 注解（见 `doc/design/web/CRequired.adoc`，校验留 web），描述与必填解耦。

== 2. 设计要点

=== 2.1 纯文档描述

- 与 `@ApiModelProperty` / `@Schema` 对齐，只承载文档描述；不再含校验相关属性。
- 必填语义迁移至 `CRequired`（web.validation）。

=== 2.2 文档联动

- `value()` 作为文档描述，配合 openapi2 的 `CSchemaAnnotationModelPropertyPlugin` 写入属性 description。

== 3. 兜底设计

| 场景 | 兜底行为 |
|------|---------|
| value 为空 | 不覆盖已有描述 |

== 4. 适用范围

=== 4.1 适用场景

- 接口属性/字段/getter 需要文档描述时标注 `@CSchema`。

=== 4.2 不适用/边界场景

- 不做运行时校验；需必填校验时配合 `@CRequired`（web.validation.annotation）。

== 变更日志

- 1.4 | 2026-09-08 | 迁入 ctool4j-definition（doc.annotation）
- 1.3 | 2026-09-06 | @Target 移除 PARAMETER（参数描述由 @CParameter 承担，CSchema 仅字段/getter 描述）
- 1.2 | 2026-09-06 | 迁入 ctool4j-doc-base（doc.annotation）
- 1.1 | 2026-09-06 | CSchema 回归纯文档描述（对齐 ApiModelProperty/@Schema）；required/message/groups/payload/@Constraint 移除，必填迁移至 CRequired
- 1.0 | 2026-08-21 | 初始落地
