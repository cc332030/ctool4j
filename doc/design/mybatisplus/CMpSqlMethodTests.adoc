= CMpSqlMethodTests 测试用例文档

Version: 1.0
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.mybatisplus.injector.CMpSqlMethod`（mybatisplus 模块 SQL 方法枚举）。

对应测试类：`com.c332030.ctool4j.mybatis.test.injector.CMpSqlMethodTests`

对应设计文档：`doc/design/mybatisplus/CMpSqlMethod.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CMpSqlMethod 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 验证 SQL 方法枚举的方法名/描述/SQL 语句定义。

=== 设计依据

- 依据功能设计对枚举值的方法名/描述/SQL 的约定。
- 依据测试方法（枚举值验证）：values、getMethod、getDesc、insertIgnoreSql、updateAllByIdSql。

=== 覆盖场景与未覆盖

- 覆盖：枚举值、方法名、描述、插入忽略 SQL、按ID更新 SQL。
- 未覆盖：真实 SQL 注入执行。

== 1. SQL 方法枚举

- 1.1 枚举值（`values`）
- 1.2 方法名（`getMethod`）
- 1.3 描述（`getDesc`）
- 1.4 插入忽略 SQL（`insertIgnoreSql`）
- 1.5 按ID更新 SQL（`updateAllByIdSql`）

== 变更日志

- 1.0 | 2026-08-21 | 初始落地
