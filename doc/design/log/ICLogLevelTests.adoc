= ICLogLevel 测试用例文档

Version: 1.0
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.log.interfaces`（log 模块）——日志级别接口族（`ICLogLevel` 及 `ICLogLevelTrace/Debug/Info/Warn/Error`）。

对应测试类：`com.c332030.ctool4j.log.test.interfaces.ICLogLevelTests`

对应设计文档：`doc/design/log/ICLogLevel.adoc` 及 `ICLogLevelTrace/Debug/Info/Warn/Error.adoc`（功能设计，测试内容以本文档为准）。

本文档为 ICLogLevel 接口族的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号，文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 覆盖各级别子接口（Trace/Debug/Info/Warn/Error）default getLevel 返回对应级别。
- 覆盖以公共父接口 ICLogLevel 引用时仍取实现级别。

=== 设计依据

- 依据功能设计对各子接口固定级别的约定。
- 依据黑盒原则与状态/引用形态：每个级别子接口 + 父接口引用均覆盖。

=== 覆盖场景与未覆盖

- 覆盖：Trace/Debug/Info/Warn/Error 五个级别的固定返回、父接口引用返回实现级别。
- 未覆盖：`ICLogLevel` 自身的裸实现（无 default，需实现方指定）。

== 1. 各级别接口 getLevel

- 1.1 Trace 返回 TRACE（getLevel_trace）
- 1.2 Debug 返回 DEBUG（getLevel_debug）
- 1.3 Info 返回 INFO（getLevel_info）
- 1.4 Warn 返回 WARN（getLevel_warn）
- 1.5 Error 返回 ERROR（getLevel_error）

== 2. 父接口引用

- 2.1 以 ICLogLevel 引用时仍取实现级别（getLevel_viaParent）

== 变更日志

- 1.0 | 2026-08-21 | 初始落地
