= CLambdaUtils 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.classes.CLambdaUtils`（core 对象类）——字段读取/写入 Lambda。

对应测试类：`com.c332030.ctool4j.core.test.classes.CLambdaUtilsTests`

对应设计文档：`doc/design/core/CLambdaUtils.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CLambdaUtils 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「读取 lambda / 写入 lambda」两个维度组织，用 ValueBean（long id 基本类型 + String name 引用类型）覆盖两种字段类型。
- 读取验证 lambda.apply 返回字段值（基本类型装箱）；写入验证 lambda.accept 设置字段值（拆箱）。

=== 设计依据

- 依据功能设计对 getter/setter lambda 的约定。

=== 覆盖场景与未覆盖

- 覆盖：基本类型字段的 get/set lambda；引用类型字段的 get/set lambda。
- 未覆盖：无（覆盖了两种字段类型的两入口）。

== 1. 读取 Lambda

- 1.1 getFieldGetLambda：基本类型（装箱）与引用类型字段读取正确（getFieldGetLambda）

== 2. 写入 Lambda

- 2.1 getFieldSetLambda：基本类型（拆箱）与引用类型字段写入正确（getFieldSetLambda）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
