= CBeanUtils 测试用例文档（CBeanUtilsTest）

Version: 1.0
LastModified: $DATE
模块：ctool4j-core
被测类：com.c332030.ctool4j.core.classes.CBeanUtils
状态：已落地

本文档为 CBeanUtilsTest 的测试用例文档，含用例设计思路与依据、覆盖场景与取舍，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.3.4`），文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 白盒原则：围绕 `CBeanUtils.copy` 属性复制能力设计用例，覆盖合并已有对象、类型不匹配、类型转换、跳过集合与 Map 等分支。
- 真实场景优先：用 `UserDto`/`UserRsp`（不同类型字段）经真实 `copy` 流程验证类型映射。

=== 覆盖场景

== 分类编号索引

=== 1. 属性复制

- 1.3.4 copyMerge：属性复制时合并已有对象
- 1.3.5 copyTypeUnmatched：属性类型不匹配时的复制行为（数值类型转换）
- 1.3.6 copySkipCollectionAndMap：属性复制时跳过集合与 Map

=== 4. 类型转换

- 4.13 copyTypeConvert：属性复制时的类型转换（sex/status/score/grade/时间）

== 变更日志

- 1.0 | $DATE | 初始落地
