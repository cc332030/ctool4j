= CSensitiveSerializer 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.jackson.CSensitiveSerializer`（core jackson 类）——敏感字段序列化器（脱敏）。

对应测试类：`com.c332030.ctool4j.core.test.jackson.CSensitiveSerializerTests`

对应设计文档：`doc/design/core/CSensitiveSerializer.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CSensitiveSerializer 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「mask 脱敏 / 序列化」两个维度组织。
- mask 覆盖默认保留、自定义保留、null、空串、短值全部打码、任意非法输入。

=== 设计依据

- 依据功能设计对脱敏规则（保留前后缀、短值全打码）的约定。

=== 覆盖场景与未覆盖

- 覆盖：默认保留前 3 后 4；自定义保留（1,1/0,4）；null；空串；短值全部打码；任意非法输入脱敏；
  序列化 null 输出 null；序列化正常脱敏。
- 未覆盖：无（覆盖了 mask 与序列化核心路径）。

== 1. mask 脱敏

- 1.1 默认保留：前 3 后 4 中间 `*`（maskDefaultKeepPrefix3Suffix4）
- 1.2 自定义保留：自定义 prefixKeep/suffixKeep（maskCustomKeep）
- 1.3 null：返回 null（maskNull）
- 1.4 空串：返回空串（maskEmptyString）
- 1.5 短值：长度不足时全部打码（maskShortValueAllMasked）
- 1.6 任意非法输入：按字符串脱敏处理（maskArbitraryIllegalInput）

== 2. 序列化

- 2.1 null 内容：输出 `null`（serializeNullContent）
- 2.2 正常内容：输出脱敏后字符串（serializeContent）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
