= CInstantDeserializer 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.jackson.deserializer.CInstantDeserializer`（core jackson 类）——Instant 反序列化。

对应测试类：`com.c332030.ctool4j.core.test.jackson.CInstantDeserializerTests`

对应设计文档：`doc/design/core/CInstantDeserializer.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CInstantDeserializer 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「字符串 / 整型毫秒 / null」多个维度组织，用 InstantBean 验证反序列化。

=== 设计依据

- 依据功能设计对字符串与整型毫秒反序列化的约定。

=== 覆盖场景与未覆盖

- 覆盖：字符串日期；整型毫秒；null 返回 null。
- 未覆盖：其他 token 回退默认实现分支（未单列）。

== 1. 反序列化

- 1.1 字符串日期：`"2025-03-03 08:01:03"` 解析非空（stringInstant）
- 1.2 整型毫秒：`1731502563000` 转 Instant（numberIntInstant）
- 1.3 null：返回 null（nullValue）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
