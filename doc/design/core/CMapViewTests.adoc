= CMapView 测试用例文档

Version: 1.0
LastModified: 2026-08-31
所属功能：`com.c332030.ctool4j.core.util.CMapView`（core 模块）——「可变 Map + 不可变视图」容器。

对应测试类：`com.c332030.ctool4j.core.test.util.CMapViewTests`

对应设计文档：`doc/design/core/CMapView.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CMapView 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号，文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 覆盖无参/指定可变实例两种构造。
- 覆盖「改可变实例 → 视图实时反映」核心语义。
- 覆盖视图只读（写入抛异常）。

=== 设计依据

- 依据功能设计对「双实例、视图为实时视图、视图只读」的约定。
- 依据白盒/黑盒原则：构造、视图实时性、只读性均覆盖。

=== 覆盖场景与未覆盖

- 覆盖：两种构造、改可变视图反映、视图只读抛异常。
- 未覆盖：并发场景（默认 ConcurrentHashMap 线程安全，未专门并发测试）。

== 1. 构造

- 1.1 无参构造可变/不可变均非空（constructDefault）
- 1.2 指定可变实例构造生效（constructWithMutable）
- 1.3 可变与不可变为不同对象（distinctInstance）

== 2. 视图实时性

- 2.1 改可变实例后视图反映最新内容（viewReflectsMutable）

== 3. 视图只读

- 3.1 对视图写入抛 UnsupportedOperationException（viewImmutable）

== 变更日志

- 1.0 | 2026-08-31 | 初始落地
