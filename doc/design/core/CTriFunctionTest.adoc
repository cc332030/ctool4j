= CTriFunction 测试用例文档

Version: 1.0
LastModified: $DATE
模块：ctool4j-core
被测类：com.c332030.ctool4j.definition.function.CTriFunction
状态：已落地

本文档为 CTriFunctionTest 的测试用例文档，含用例设计思路与依据、覆盖场景与取舍，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 2.1`），文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 白盒原则：围绕 CTriFunction 内置常量（取第一/第二/第三个参数）的行为设计用例，覆盖不同参数类型。

=== 覆盖场景

== 分类编号索引

=== 2. 内置函数常量

- 2.1 取第一个参数
- 2.2 取第二个参数
- 2.3 取第三个参数

== 变更日志

- 1.0 | $DATE | 初始落地
