= CClassConverter 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.classes.CClassConverter`（core 对象类）——类型转换器定义。

对应测试类：`com.c332030.ctool4j.core.test.classes.CClassConverterTests`

对应设计文档：`doc/design/core/CClassConverter.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CClassConverter 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「builder / 全参构造 / 无参构造」三个构造入口组织。
- builder 与全参构造验证字段正确且转换函数可用；无参构造验证字段为 null。

=== 设计依据

- 依据功能设计对三种构造方式的约定。

=== 覆盖场景与未覆盖

- 覆盖：builder 构造字段与转换；全参构造；无参构造字段 null。
- 未覆盖：无（覆盖了全部构造）。

== 1. 构造

- 1.1 builder：fromClass/toClass/converter 正确（build）
- 1.2 全参构造：字段正确且转换可用（allArgsConstructor）
- 1.3 无参构造：字段为 null（noArgsConstructor）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
