= CCompletableFuture 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.util.CCompletableFuture`（core 工具类）——异步执行任务，失败记录日志且保留异常完成状态。

对应测试类：`com.c332030.ctool4j.core.test.util.CCompletableFutureTests`

对应设计文档：`doc/design/core/CCompletableFuture.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CCompletableFuture 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「正常执行 / 异常传播」两个维度组织。
- 正常执行：任务自增 AtomicInteger，get() 返回 null 且自增生效，验证异步执行成功。
- 异常传播：任务抛异常，断言 get() 抛 ExecutionException，且 Future 处于异常完成状态
  （isDone 与 isCompletedExceptionally 为 true），验证不吞异常的核心语义。

=== 设计依据

- 依据功能设计对不吞异常、保留异常完成状态的约定。
- 依据测试方法（等价类/边界值/异步时序）：正常任务、异常任务、完成状态断言。

=== 覆盖场景与未覆盖

- 覆盖：正常异步执行（结果 null + 副作用生效）、异常传播（get 抛 ExecutionException、isDone、
  isCompletedExceptionally）。
- 未覆盖：任务耗时超时、拒绝策略等（非本类关注点）。

== 1. 正常执行

- 1.1 异步执行：任务自增生效，get() 返回 null（runAsyncNormal）

== 2. 异常传播

- 2.1 异常保持：任务抛异常，get() 抛 ExecutionException，isDone 与 isCompletedExceptionally 为 true（runAsyncExceptionPropagated）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
