= CEnumUtils 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.util.CEnumUtils`（core 工具类）——枚举按名/值/指定字段取值的 Map 构建、枚举值反查、枚举值列表获取。

对应测试类：`com.c332030.ctool4j.core.test.util.CEnumUtilsTests`

对应设计文档：`doc/design/core/CEnumUtils.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CEnumUtils 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「Map 构建 / 反查 / 异常路径 / 值列表」四个维度组织。
- Map 构建覆盖 getNameMap、getMap（按字段名），并验证 Map 大小与内容。
- 反查覆盖 nameOf / valueOf（map、字段名），以及各入口的不存在异常路径。
- 异常路径用 `assertThrowsExactly` 精确匹配异常类型：字段不存在抛 `NoSuchFieldException`、
  非枚举抛 `IllegalArgumentException`、反查不存在抛 `IllegalArgumentException`。
- 测试数据用 `CDbOperateEnum`（含 name 与 text 字段），贴近真实枚举使用场景。

=== 设计依据

- 依据功能设计对 Map 构建规则（name 用 `Enum.name()`、字段值用反射取值且 null 过滤）与反查抛异常约定。
- 依据测试方法（等价类/边界值/分支覆盖/异常路径）：正例反查、不存在反查、非枚举、字段不存在。
- `assertThrowsExactly` 精确匹配异常类型（依据测试规范，不用 `assertThrows` 掩盖子类语义）。

=== 覆盖场景与未覆盖

- 覆盖：getNameMap（按 name）、getMap 按字段名（text）、字段不存在抛 NoSuchFieldException、非枚举抛
  IllegalArgumentException、valueOf(map)/nameOf/valueOf(字段名) 正例与不存在抛异常、values 值列表。
- 未覆盖：字段值为 null 的枚举不建反查项（`CDbOperateEnum` 无 null 值字段，属构建规则边界，设计已记录）。

== 1. Map 构建

- 1.1 getNameMap：按枚举名构建，含全部枚举（getNameMap）
- 1.2 getMap 按字段名：按 text 字段构建，值映射正确（getMapByFieldName）
- 1.3 异常：字段不存在抛 NoSuchFieldException（getMapByUnknownFieldThrows）
- 1.4 异常：非枚举类抛 IllegalArgumentException（getMapNotEnumThrows）

== 2. 反查

- 2.1 valueOf(map, value)：从 Map 反查命中（valueOfByMap）
- 2.2 异常：valueOf(map, value) 值不存在抛 IllegalArgumentException（valueOfByMapNotFoundThrows）
- 2.3 nameOf：按枚举名反查命中（nameOf）
- 2.4 异常：nameOf 枚举名不存在抛 IllegalArgumentException（nameOfNotFoundThrows）
- 2.5 valueOf(Class, fieldName, value)：按字段值反查命中（valueOfByFieldName）
- 2.6 异常：valueOf(Class, fieldName, value) 值不存在抛 IllegalArgumentException（valueOfByFieldNameNotFoundThrows）

== 3. 值列表（values）

- 3.1 values：返回全部枚举值列表且顺序正确（values）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
