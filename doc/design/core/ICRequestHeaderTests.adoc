= ICRequestHeader 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.interfaces.ICRequestHeader`（core 接口类）——请求报文头定义接口（默认数据类型/必输/报文头名）。

对应测试类：`com.c332030.ctool4j.core.test.interfaces.ICRequestHeaderTests`

对应设计文档：`doc/design/core/ICRequestHeader.adoc`（功能设计，测试内容以本文档为准）。

本文档为 ICRequestHeader 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「默认行为 / 覆盖行为」两个维度组织。
- 默认：匿名实现仅提供 name/text，验证 dataType 默认 STRING、required 默认 false、Header 名推导。
- 覆盖：覆盖 dataType/required，验证自定义值与 Header 名推导。

=== 设计依据

- 依据功能设计对默认值（STRING/false）与 Header 名推导的约定。
- 依据测试方法（等价类/分支覆盖）：默认分支、覆盖分支、Header 名。

=== 覆盖场景与未覆盖

- 覆盖：默认 dataType/required/Header 名（TRACE_ID→Trace-Id）；覆盖 dataType/required 及 Header 名
  （X_TOKEN→X-Token）。
- 未覆盖：无（覆盖了默认与覆盖两分支）。

== 1. 默认行为

- 1.1 defaults：dataType=STRING、required=false、Header 名 `Trace-Id`、text（defaults）

== 2. 覆盖行为

- 2.1 override：dataType=LONG、required=true、Header 名 `X-Token`（override）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
