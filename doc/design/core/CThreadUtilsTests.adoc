= CThreadUtils 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.util.CThreadUtils`（core 工具类）——创建守护线程（默认/自定义命名）。

对应测试类：`com.c332030.ctool4j.core.test.util.CThreadUtilsTests`

对应设计文档：`doc/design/core/CThreadUtils.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CThreadUtils 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「命名重载 / 默认命名 / 可执行性」三个维度组织。
- 命名重载验证线程名与 daemon 标志；默认命名验证前缀 `DaemonThread-` 与 daemon。
- 可执行性：启动线程后经 CountDownLatch 验证任务实际执行，且线程为 daemon。

=== 设计依据

- 依据功能设计对 daemon 与默认命名的约定。
- 依据测试方法（等价类/边界值）：命名/默认命名、任务执行验证。

=== 覆盖场景与未覆盖

- 覆盖：自定义命名、默认命名（前缀校验）、任务可执行且线程为 daemon。
- 未覆盖：不阻止 JVM 退出的端到端验证（daemon 语义由 `isDaemon()` 间接保证）。

== 1. 创建守护线程

- 1.1 命名重载：线程名正确且为 daemon（newDaemonThreadWithName）
- 1.2 默认命名：前缀为 `DaemonThread-` 且为 daemon（newDaemonThreadDefaultName）
- 1.3 可执行性：启动后任务执行成功（CountDownLatch）且为 daemon（newDaemonThreadRunnableExecutes）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
