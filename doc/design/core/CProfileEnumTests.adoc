= CProfileEnum 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.enums.CProfileEnum`（core 枚举类）——环境枚举（含按名获取、生产环境集合）。

对应测试类：`com.c332030.ctool4j.core.test.enums.CProfileEnumTests`

对应设计文档：`doc/design/core/CProfileEnum.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CProfileEnum 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「按名获取 / 全枚举 / 未知异常 / 生产集合 / 描述」多个维度组织。
- 按名获取覆盖大小写不敏感；全枚举覆盖每个环境；未知异常覆盖未知名/近似名/空/null；
  生产集合验证仅含 PROD；描述覆盖默认与生产。

=== 设计依据

- 依据功能设计对忽略大小写反查、未知抛异常、PROD_PROFILES 仅含 PROD 的约定。
- 依据测试方法（等价类/边界值/异常路径）：大小写、全枚举、未知/空/null。

=== 覆盖场景与未覆盖

- 覆盖：of 大小写不敏感；6 个环境按名获取；未知/近似/空/null 抛异常；PROD_PROFILES 含 PROD 不含 DEV；
  描述。
- 未覆盖：无（覆盖了全部入口与边界）。

== 1. 按名获取（of）

- 1.1 大小写不敏感：`default`/`DEFAULT` 均返回 DEFAULT（of）
- 1.2 全枚举：6 个环境按名获取（ofAllEnums）
- 1.3 未知异常：`UNKNOWN`/`prod1`/空/null 抛 IllegalArgumentException（ofUnknownThrows）

== 2. 生产环境集合

- 2.1 PROD_PROFILES：大小 1 仅含 PROD、不含 DEV（prodProfiles）

== 3. 描述

- 3.1 text：DEFAULT 为 `默认`、PROD 为 `生产`（text）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
