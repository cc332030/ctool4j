= CLogBlobSerializerModifier 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.jackson.CLogBlobSerializerModifier`（core jackson 类）——长文本字段日志占位符修改器。

对应测试类：`com.c332030.ctool4j.core.test.jackson.CLogBlobSerializerModifierTests`

对应设计文档：`doc/design/core/CLogBlobSerializerModifier.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CLogBlobSerializerModifier 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「日志 mapper 占位 / 全局 mapper 真实 / 深拷贝隔离 / 普通字段 / 无注解 / null 跳过」多个维度组织。
- 用 BlobBean（name + @CLogBlob content）与 PlainBean 验证各场景。

=== 设计依据

- 依据功能设计对仅日志 mapper 生效、占位符输出的约定。

=== 覆盖场景与未覆盖

- 覆盖：日志 mapper 输出 `<BLOB>`；全局 mapper 输出真实内容；深拷贝不影响其他 mapper；普通字段不受影响；
  无注解 bean 正常；@CLogBlob null 字段日志 mapper 不输出。
- 未覆盖：无（覆盖了核心行为）。

== 1. 占位符替换

- 1.1 日志 mapper：@CLogBlob 字段输出 `<BLOB>`（blobFieldSerializedToPlaceholder）
- 1.2 全局 mapper：@CLogBlob 字段输出真实内容（globalMapperOutputsRealContent）
- 1.3 深拷贝隔离：日志 mapper 注册不影响其他 mapper（logMapperDeepCopyDoesNotAffectOthers）
- 1.4 普通字段：name 正常输出（normalFieldNotAffected）
- 1.5 无注解 bean：正常输出（noBlobBeanNormal）
- 1.6 null 字段：日志 mapper 不输出（logMapperSkipsNullField）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
