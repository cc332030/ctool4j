= CLogUtils 测试用例文档

Version: 1.0
LastModified: $DATE
模块：ctool4j-core
被测类：com.c332030.ctool4j.core.log.CLogUtils
状态：已落地

本文档为 CLogUtilsTest 的测试用例文档，含用例设计思路与依据、覆盖场景与取舍，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位。

== 用例设计思路与依据

=== 设计思路

- 白盒原则：围绕 `isJsonLog`（判断类型是否以 JSON 格式记录日志）设计用例，覆盖是 JSON 类型（如 `CResult`）与不是 JSON 类型（如 `String`、`DataSource`）两类。

=== 覆盖场景

== 分类编号索引

=== 1. JSON 日志判断

- 1.1 isJsonLog：CResult 为 true，String/DataSource 为 false

== 变更日志

- 1.0 | $DATE | 初始落地
