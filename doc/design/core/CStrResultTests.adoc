= CStrResult 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.definition.model.result.impl.CStrResult`（definition 模型类）——String 状态码结果封装。

对应测试类：`com.c332030.ctool4j.definition.test.model.result.impl.CStrResultTests`

对应设计文档：`doc/design/core/CStrResult.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CStrResult 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「构造 / 成功工厂 / 失败工厂 / builder」多个维度组织，验证工厂方法与字段。

=== 设计依据

- 依据功能设计对 success/error、message null 兜底的约定。

=== 覆盖场景与未覆盖

- 覆盖：无参/全参构造；newInstance（含 null）；success；error（默认/自定义 code/HttpStatus）。
- 未覆盖：无。

== 1. 构造

- 1.1 无参构造（noArgsConstructor）
- 1.2 全参构造（allArgsConstructor）
- 1.3 newInstance（newInstance）
- 1.4 newInstance null 参数（newInstanceNullParams）

== 2. 成功工厂

- 2.1 success()（success）
- 2.2 success(data)（successWithData）
- 2.3 success(httpStatus, data)（successWithHttpStatus）

== 3. 失败工厂

- 3.1 error(message)（errorWithMessage）
- 3.2 error(null)（errorNullMessage）
- 3.3 error(code, message)（errorWithCodeAndMessage）
- 3.4 error(httpStatus, message)（errorWithHttpStatusAndMessage）
- 3.5 error(httpStatus)（errorWithHttpStatusOnly）

== 4. builder

- 4.1 builder（builder）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
