= ICBusinessExceptionProvider 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.exception.ICBusinessExceptionProvider`（core 异常接口）——业务异常提供者接口（SPI 扩展点）。

对应测试类：`com.c332030.ctool4j.core.test.exception.ICBusinessExceptionProviderTests`

对应设计文档：`doc/design/core/ICBusinessExceptionProvider.adoc`（功能设计，测试内容以本文档为准）。

本文档为 ICBusinessExceptionProvider 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「默认 getMessageExceptionFunction 抛异常 / 委托创建 / null error」三个维度组织。
- 用匿名实现覆盖 getMessageExceptionFunction 返回指定异常类型，验证 getExceptionFunction 委托。

=== 设计依据

- 依据功能设计对默认抛 UnsupportedOperationException 与委托 formatResMessage 的约定。

=== 覆盖场景与未覆盖

- 覆盖：默认 getMessageExceptionFunction 抛 UnsupportedOperationException；覆盖后 getExceptionFunction
  委托创建异常（含 cause）；error 为 null 时消息仅含扩展信息。
- 未覆盖：无（覆盖了默认异常与委托分支）。

== 1. 默认 getMessageExceptionFunction

- 1.1 默认调用抛 UnsupportedOperationException（defaultGetMessageExceptionFunction_throws）

== 2. 委托创建（getExceptionFunction）

- 2.1 覆盖后创建：`[200] ok: detail`，cause 正确（getExceptionFunction_delegates）
- 2.2 error 为 null：消息仅含 `only-extend`（getExceptionFunction_nullRes）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
