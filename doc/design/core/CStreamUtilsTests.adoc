= CStreamUtils 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.core.util.CStreamUtils`（core 工具类）——Stream 按 key 去重谓词（并发安全）。

对应测试类：`com.c332030.ctool4j.core.test.util.CStreamUtilsTests`

对应设计文档：`doc/design/core/CStreamUtils.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CStreamUtils 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「去重正例 / null key 过滤 / 单元素」三个维度组织。
- 去重正例：重复元素只保留首个，验证去重结果与顺序。
- null key：key 提取返回 null 的元素被全部过滤（结果为空）。
- 单元素：单元素流不被误过滤。

=== 设计依据

- 依据功能设计对去重语义与 null key 过滤的约定。
- 依据测试方法（等价类/边界值）：重复元素、null key、单元素边界。

=== 覆盖场景与未覆盖

- 覆盖：去重正例（含重复元素、保持首次出现顺序）、null key 过滤、单元素保留。
- 未覆盖：并发环境下的多线程去重竞态（ConcurrentHashMap 保证，单测不构造并发场景）。

== 1. 去重

- 1.1 正例：`["a","b","a","c","b"]` 去重 → `[a,b,c]`（distinctByKey）
- 1.2 边界：key 均为 null 时全部过滤，结果为空（distinctByKeyNullKeyExcluded）
- 1.3 边界：单元素流不被过滤（distinctByKeySingleElement）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
