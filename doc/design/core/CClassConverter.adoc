= CClassConverter 设计文档

Version: 1.0
LastModified: 2026-08-21
模块：ctool4j-core
类：com.c332030.ctool4j.core.classes.CClassConverter
状态：已落地
适用范围：类型转换器定义（源类/目标类/转换函数）

== 1. 功能说明

`CClassConverter<From, To>` 为类型转换器定义类，含：

- `fromClass`：源类
- `toClass`：目标类
- `converter`：转换函数（CFunction<From, To>）

标注 `@Data`/`@SuperBuilder`/`@NoArgsConstructor`/`@AllArgsConstructor`，支持 builder 与三种构造。

== 2. 设计要点

=== 2.1 结构

- 通过 Lombok 生成 getter/setter、builder、无参/全参构造，供 `CConvertUtils` 注册转换器使用。

== 3. 兜底设计

| 场景 | 兜底行为 |
|------|---------|
| 无参构造 | 三字段均为 null |

== 4. 适用范围

=== 4.1 适用场景

- 描述源类型→目标类型的转换关系，供 CConvertUtils 注册与查找。

=== 4.2 不适用/边界场景

- 纯数据结构，无行为逻辑。

== 5. 已知限制与取舍

- 简单数据载体，配合 builder 便捷构造。

测试内容见 `doc/design/core/CClassConverterTests.adoc`。

== 变更日志

- 1.0 | 2026-08-21 | 初始落地
