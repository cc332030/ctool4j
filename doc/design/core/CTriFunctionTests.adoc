= CTriFunction 测试用例文档

Version: 1.1
LastModified: 2026-08-21
所属功能：`com.c332030.ctool4j.definition.function.CTriFunction`（definition 函数接口）——可抛受检异常的三参函数接口。

对应测试类：`com.c332030.ctool4j.definition.test.function.CTriFunctionTests`

对应设计文档：`doc/design/core/CTriFunction.adoc`（功能设计，测试内容以本文档为准）。

本文档为 CTriFunction 的测试用例文档，含用例设计思路与依据、覆盖场景，以及测试用例的分类编号索引；测试方法在 javadoc 中标注对应编号（如 `对应测试用例 1.1`），文档与代码互相定位，便于检查覆盖是否有遗漏。

== 用例设计思路与依据

=== 设计思路

- 按「apply / 工具方法」两个维度组织。
- apply 覆盖正常、null 输入、受检异常；工具覆盖 first/second/third、静态 apply（null/正常）、convert（null/正常）。

=== 设计依据

- 依据功能设计对 @SneakyThrows 包装与工具方法的约定。

=== 覆盖场景与未覆盖

- 覆盖：apply 正常/null/受检异常；first/second/third；静态 apply null/正常；convert null/正常。
- 未覆盖：无。

== 1. apply

- 1.1 正常：结果正确（applyNormal）
- 1.2 null 输入：正常处理（applyNullInputs）
- 1.3 受检异常：抛 IOException（applySneakyThrowsCheckedException）

== 2. 工具方法

- 2.1 first：返回第一参数（first）
- 2.2 second：返回第二参数（second）
- 2.3 third：返回第三参数（third）
- 2.4 静态 apply null：返回 null（staticApplyNullFunction）
- 2.5 静态 apply 正常：结果正确（staticApplyNormal）
- 2.6 convert：转换（convert）
- 2.7 convert null：返回 null（convertNullCTriFunction）

== 变更日志

- 1.1 | 2026-08-21 | 测试用例编号改为在测试方法 javadoc 中标注（符合 AGENTS-TEST.MD 规范）；补充 Version/LastModified 头部字段
- 1.0 | 2026-08-21 | 初始落地
